###
##
#
This is a basic  port scanner that checks whether the given range of ports that  are open or closed on a given target.
you can see the code in the python code file in this repository.
As per my convinient i mentioned the ports range 1-43 cause it takes less time to scan and the target is "scanme.nmap.org".
*****
***
**
* # Note that nmap is my target cause it is basic scan, and note that without permessions not suposed to do any actions scans or attacks it is unethical.
As per results the port 22 is open PortService 22 SSH.
** Screenshots are attached by the results.
